# Phase 19 — Gate 19.7 Recovery V2 Plan Admission

## Status

**RECOVERY PLAN V2 ADMITTED — APPLY NOT AUTHORIZED**

The post-untaint recovery plan was generated by the HUMAN operator from the exact protected-main checkpoint:

```text
source_head_sha: d6546e9d48253694e3e276940ebd2388f301d8ad
post-untaint state lineage: 6c958ab2-4cc6-7f96-a528-89535504f65c
post-untaint state serial: 116
```

The pre-plan state check confirmed the API Lambda instance remained `READY_OR_UNMARKED`; the previous failed plan and rejected replacement plan remained quarantined and non-reusable.

## Fresh recovery plan

```text
Plan: 5 to add, 1 to change, 0 to destroy.
```

Exact managed creates:

```text
aws_apigatewayv2_integration.public_async_api_lambda
aws_apigatewayv2_route.public_async_result
aws_apigatewayv2_route.public_async_status
aws_apigatewayv2_route.public_async_submit
aws_lambda_permission.public_async_api_gateway
```

The only managed update is:

```text
aws_lambda_function.public_async_api
reserved_concurrent_executions: -1 -> 0
```

There are no managed deletes and no replacements.

Terraform also reported the provider-populated `invoke_arn` as an object change observed during refresh. That observation did not expand the managed action set and is not treated as new execution authority.

## Offline admission

```text
phase19_gate19_7_recovery_plan=PASS managed_creates=5 managed_updates=1 deletes=0 replacements=0 plan_binary_sha256=48cb15a8466cc0ce77fdbe1d827b5a0aebb88460b8e8151d8c88d3c85eb9ea55 api_reserved_concurrency=0 worker_reserved_concurrency=0 materialized_not_enabled=true terraform_apply_authorized=false human_apply_authorization_required=true
```

Bounded hashes:

```text
source_head_sha:    d6546e9d48253694e3e276940ebd2388f301d8ad
plan_binary_sha256: 48cb15a8466cc0ce77fdbe1d827b5a0aebb88460b8e8151d8c88d3c85eb9ea55
plan_json_sha256:   8a1f95ab8cbcbf96c708c6ced0d0127d862391a6f8e72c250fe5e7499d2a0bf7
terraform_version:  1.15.8
terraform_format:   1.2
account_id:          487757851499
region:              us-east-1
```

Canonical bounded evidence:

```text
labs/evidence/phase-19-gate-19-7-recovery-v2-plan-admission-v1.json
```

The Terraform binary plan and full provider JSON remain local-only and must not be committed.

## Immutable artifact provenance

The recovery plan retained the exact immutable Gate 19.5 artifacts:

```text
API VersionId:    E.jfB7dlkGCD.wHAurP7QXo4fuS_PW63
API source hash:  mUd2dtzEE0XGPtKMgbtBx/n0e89bByJUvB7w4s/Nh24=
Worker VersionId: sxiOdii4yFwR13t23xP5A8EU1JPV_7P1
Worker source hash: DQS0ckdq14JbUZA1LaFkLbmn1C0c40nSGjn6yPbsvck=
```

No artifact publication occurred.

## Safety result

The recovery-plan verifier proved:

```text
managed_creates: 5
managed_updates: 1
managed_deletes: 0
managed_replacements: 0
api_reserved_concurrency: 0
worker_reserved_concurrency: 0
public_endpoint_enablements: 0
provider_heavy_public_executions: 0
materialized_not_enabled: true
terraform_apply_authorized: false
apply_authorization_status: PENDING_EXPLICIT_HUMAN_AUTHORIZATION
```

The retained disabled/non-public boundary remains controlling. In particular:

```text
plan != apply
materialized != enabled
failed plan != retry authority
state reconciliation != AWS mutation
```

## Mandatory next boundary

This bounded admission evidence must be protected-merged and exact-main post-merge verification must succeed before apply authority can even be considered.

After that protected checkpoint, a **new explicit HUMAN apply authorization** must bind all three exact values:

```text
source_head_sha
plan_binary_sha256
plan_json_sha256
```

The authorization must also bind the admitted action shape:

```text
5 creates
1 in-place API Lambda update to reserved_concurrent_executions = 0
0 deletes
0 replacements
```

Until such authorization is explicitly recorded, do not run `terraform apply`.

The standard Terraform message suggesting `terraform apply <saved-plan>` is not OpsLens execution authority.

## Explicit non-authority

This admission does not authorize:

```text
terraform apply
additional Terraform state mutation
replacement or destroy
runtime/public endpoint enablement
submit switch enablement
worker switch enablement
event-source enablement
IAM broadening
provider-heavy execution
custom public domain creation
PR #89 modification
third-party repository execution
```
