# OpsLens Demo

This directory will contain the canonical V1 demonstration entry point and supporting assets.

The canonical V1 path will be offline-first and deterministic. It must reuse retained OpsLens authorities and must never execute third-party repository code.

Planned completion slices:

```text
Gate 19.10  deterministic end-to-end demo runner
Gate 19.11  curated scenarios + deterministic evaluation
Gate 19.12  minimal local visual demo
```

Until Gate 19.10 is implemented, this file is scope documentation only and grants no runtime/provider authority.
